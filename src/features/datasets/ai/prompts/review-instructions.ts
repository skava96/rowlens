export const AI_REVIEW_INSTRUCTIONS = {
  task: "business_level_pattern_discovery",
  maxFindings: 2,
  deterministicValidationComplete: true,
  allowedCategories: ["pattern", "business_context", "review_priority"],
  requiredFindingFields: [
    "title",
    "description",
    "reasoning",
    "confidence",
    "category",
    "targetField",
    "affectedRows",
    "suggestedAction",
  ],
  allowedSuggestedActions: ["flag_invalid", "standardize_value"],
  rules: [
    "Return at most 2 findings.",
    "Prefer 1 strong finding over 2 weak findings.",
    "Do not repeat deterministic validation results.",
    "Use Column Evidence as the primary source of truth.",
    "Use Sample Rows only as examples.",
    "Each finding must use exactly one targetField.",
    "targetField must equal one field from Column Evidence.",
    "Return selectedValues instead of affectedRows.",
    "selectedValues must be exact values from topValues.value for the same targetField.",
    "Do not invent selectedValues.",
    "The application will calculate affectedRows from selectedValues.",
    "Never invent fields, values, or row IDs.",
    "Never include rows from unrelated values.",
    "Never include all rows unless every row is directly affected.",
    "Description must be one sentence.",
    "Reasoning must be one sentence.",
    "Use standardize_value for naming, casing, aliases, abbreviations, or terminology inconsistencies.",
    "Use flag_invalid only for suspicious values or numeric outliers.",
  ],
} as const;